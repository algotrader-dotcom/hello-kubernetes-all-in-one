server {
    server_name fathom.puppy.vn;

    listen 80;
    #if ($host = fathom.puppy.vn) {
    #    return 301 https://$host$request_uri;
    #}

    #listen 443 ssl; # managed by Certbot
    #ssl_certificate /etc/letsencrypt/live/fathom.puppy.vn/fullchain.pem; # managed by Certbot
    #ssl_certificate_key /etc/letsencrypt/live/fathom.puppy.vn/privkey.pem; # managed by Certbot
    #include /etc/letsencrypt/options-ssl-nginx.conf; # managed by Certbot
    #ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem; # managed by Certbot

    location / {
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $remote_addr;
            proxy_set_header Host $host;
            proxy_pass http://10.0.3.9:9000;
    }
}